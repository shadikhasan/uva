#include <bits/stdc++.h>

using namespace std;
long long int skewbinary(string n)
{
    long long int sB = 0, d;
    int i = 1, j = 1, l;
    l = n.size();
    while(!n.empty())
    {
        d = pow(2, i) - 1;
        sb += n[]
    }

    return sB;
}
int main()
{
    string n;
    while(1)
    {
        cin >> n;
        if(n == "0")break;

        cout << skewbinary(n) << endl;
    }

}

